
<snippet>
    <content><![CDATA[
# instantiate documents object
documents = tc.documents()

# create an empty document
document = documents.add('', owner)
# specify the ID of the document you wish to delete
document.set_id(${1:<Existing document ID>})

try:
    # delete the document
    document.delete()
except RuntimeError as e:
    print('Error: {0}'.format(e))
    sys.exit(1)

]]></content>
    <tabTrigger>tcddocuments</tabTrigger>
    <scope>source.python</scope>
</snippet>
