
<snippet>
    <content><![CDATA[
# instantiate campaigns object
campaigns = tc.campaigns()

# create an empty campaign
campaign = campaigns.add('', owner)
# specify the ID of the campaign you wish to delete
campaign.set_id(${1:<Existing campaign ID>})

try:
    # delete the campaign
    campaign.delete()
except RuntimeError as e:
    print('Error: {0}'.format(e))
    sys.exit(1)

]]></content>
    <tabTrigger>tcdcampaigns</tabTrigger>
    <scope>source.python</scope>
</snippet>
